#include <stdio.h>
#include <stdlib.h>
/**
 * main -main function to print the lowercase alphabet in reverse
 * Return: Always 0
*/
int main(void)
{
	char n = 'z';

	while (n >= 'z')
	{
		putchar(z);
		n--;
	}
	putchar('\n');
	return (0);
}
