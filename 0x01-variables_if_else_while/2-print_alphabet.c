#include <stdio.h>
#include <stdlib.h>
/**
 * main -main funtion to prints the alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	int n = "a";

	for (n = "a"; n <= "z"; n++)
	{
		putchar(n);
	}

	return (0);

}
